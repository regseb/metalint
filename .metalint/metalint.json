{
    "patterns": ["**", "!.git/**", "!node_modules/**", "!coverage/**"],
    "hidden": true,
    "checkers": [
        {
            "patterns": "**/*.js",
            "linters": ["jscs", "eslint"]
        }, {
            "patterns": "**/*.md",
            "linters": "markdownlint"
        }, {
            "patterns": "**/*.json",
            "linters": { "jsonlint": { "comments": false } }
        }
    ]
}
